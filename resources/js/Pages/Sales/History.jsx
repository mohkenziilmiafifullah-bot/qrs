import SalesLayout from '@/Layouts/SalesLayout';
export default function History({ qrs }) { return <SalesLayout title="Riwayat Aktivasi"><section className="history-intro"><p className="section-kicker">TOKO TERHUBUNG</p><h2>Riwayat aktivasi QR</h2><p>Daftar toko yang QR review-nya telah aktif.</p></section><section className="sales-list-section">{qrs.data.length === 0 ? <div className="sales-empty">Belum ada riwayat aktivasi.</div> : qrs.data.map((qr) => <article className="sales-row" key={qr.code}><span className="row-mark">✓</span><div><b>{qr.merchant_name}</b><small>{qr.code} · {new Date(qr.activated_at).toLocaleString('id-ID')}</small></div><span className="active-pill">Aktif</span></article>)}</section></SalesLayout>; }
